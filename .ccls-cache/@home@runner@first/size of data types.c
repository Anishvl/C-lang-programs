#include <stdio.h>
#include "gstio.h"
//int main(void)
void main()
{
 char a;
  int b;
  float c;
  double d;
  printf("sizeof the value is %d",sizeof(a));
  printf("\nsizeof the value is %d",sizeof(b));
  printf("\nsizeof the value is %d",sizeof(c));
  printf("\nsizeof the value of %d",sizeof(d));
  printf("\n\n coded and developed by Anishvl");
  return 0;
}